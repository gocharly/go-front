---
name: go-front
description: >-
  Архитектурный стандарт текстовой инженерии, редактуры и очистки текстов от штампов, канцелярита, шаблонных AI-конструкций и воды. Применяется для публичных текстов, статей, постов, документации и UI/UX микрокопий. Включает автономный скрипт-линтер (scan_slop.py), реестр 25+ запрещенных конструкций (Hard Bans), факт-замок и модульные справочники. Применяется при запросах: очеловечь, вычитай, редактор, убери канцелярит, проверь текст, humanizer, инфостиль.
---

# GO-FRONT: Стандарт текстовой инженерии и чистоты текста

## Роль и назначение
**GO-FRONT** — операционный диспетчер и линтер для очистки текстов, коммитов, логов и UI от штампов, канцелярита, воды и шаблонных фраз.

Главный принцип: **Удаляй, а не дописывай. Факт первичен перед стилем. Механическая верификация через линтер перед сдачей.**

### Что делает стандарт:
1. **Редактирует и сокращает**: срезает штампы, шаблонные вводные слова, канцелярит и искусственные формулы.
2. **Исправляет типографику**: меняет длинные тире на дефис `-`, кавычки-елочки на прямые `" "`, убирает эмодзи.
3. **Держит факт-замок**: строго запрещает придумывать факты, цифры, проценты и кейсы, которых нет в источнике.
4. **Проверяет качество**: верифицирует текст через внутренний скрипт-линтер.

### Чего стандарт НЕ делает:
1. Не меняет первоначальный смысл и фактуру текста.
2. Не дописывает текст абстрактными рассуждениями ради объема.
3. Не оставляет непроверенных правок.

---

## 1. Операционный рельсовый конвейер (State Machine)

Работа агента строится строго по 3 фазам:

```text
[ФАЗА 1: МАРШРУТИЗАЦИЯ И СПРАВОЧНИК]
  └── Определение типа задачи -> подключение только профильного файла из references/
        │
        ▼
[ФАЗА 2: ХИРУРГИЧЕСКАЯ ПРАВКА]
  └── Срез штампов, факт-замок (запрет выдумывать цифры), естественный ритм
        │
        ▼
[ФАЗА 3: МЕХАНИЧЕСКАЯ ВЕРИФИКАЦИЯ (LINTER)]
  └── Запуск линтера scan_slop.py -> проверка нарушений -> достижение скора 100/100
```

---

## 2. Команды и режимы работы

1. **Редактура (`/edit` или по умолчанию)**:
   - Полная зачистка текста от запрещенных шаблонов, монотонности и воды.
   - Запуск: `python3 /Users/tmpr/.agents/skills/go-front/scripts/scan_slop.py <file> --fix`
   - Ручная доводка смысловых штампов до получения скора `100/100`.
   - Вывод: чистый текст. Плашка линтера используется строго для внутренней верификации и не выводится в чат.

2. **Аудит и скоринг (`/audit`)**:
   - Запуск строго по прямому запросу пользователя.
   - Запуск: `python3 /Users/tmpr/.agents/skills/go-front/scripts/scan_slop.py <file>`
   - Вывод: численный скор (0-100) и построчный список нарушений.

3. **Точечная типографика (`/fix`)**:
   - Автоматическая замена длинных тире на дефис `-`, кавычек-елочек на `" "`, удаление эмодзи.
   - Запуск: `python3 /Users/tmpr/.agents/skills/go-front/scripts/scan_slop.py <file> --fix`

---

## 3. Базовые правила (Hard Bans)

Любой текст обязан проходить проверку на отсутствие следующих маркеров:
- Запрещены формулы: `не просто X, а Y`, `не только X, но и Y`, `дело не в...`.
- Запрещены зачины: `В современном мире`, `В эпоху...`, `Стоит отметить, что...`, `Важно понимать`.
- Запрещен канцелярит: `данный`, связка `является`, `играет ключевую роль`.
- Запрещен шум: `Давайте разберемся`, `Погрузимся в`, `И это нормально`, `Надеюсь, статья помогла`.
- Запрещены длинные тире вне чисел (заменяются на дефис `-` или запятую) и кавычки-елочки (заменяются на прямые кавычки).
- **Факт-замок**: Строгий запрет выдумывать цифры, проценты, даты и кейсы, которых нет в источнике.

---

## 4. Модульные справочники (References)

Для глубоких задач агент подгружает только нужный модуль:

* [Реестр 25+ запрещенных шаблонов и конструкций](./references/markers-and-hard-bans.md) — полный каталог паттернов и замен.
* [Структурные шаблоны и анти-паттерны](./references/structural-patterns.md) — бинарные контрасты, негативные списки, фрагментация.
* [Лаконичность ответов и точность формулировок](./references/llm-logistics-and-anti-compensation.md) — защита от раздувания текста и галлюцинаций.
* [Библиотека эталонных калибровок](./references/few-shot-library.md) — пары «Шаблонный текст → Чистый текст».
* [Инженерия и физический реализм](./references/ai-coding-and-hardware-reality.md) — Data-Oriented Design, кэш процессора, RAG-гигиена.
* [Типографика и микрокопии UI](./references/typography-and-ui-microcopy.md) — шрифты, правила контраста и шаблоны строк настроек.
* [Верифицированные UI/UX ресурсы, иконки и дизайн-системы](./references/verified-ui-ux-resources.md) — реестр проверенных библиотек с актуальными звездами GitHub и командами установки под React, Vue, Svelte, Flutter.
* [Когнитивная эргономика, минимализм и архитектура фокуса](./references/cognitive-ergonomics-and-minimalism.md) — лимит Миллера (4-7 элементов), правило единого якоря (Single Anchor), прогрессивное раскрытие, чек-лист аудита экрана и запрет эмодзи.
* [Упрощение кода и организация функций](./references/code-simplification-and-functions.md) — модульность `libs/` против свалки `utils`, именование по действию, разделение бизнес-логики и чек-лист структуры.
* [Архитектура backend и проектирование API](./references/backend-architecture-and-api.md) — валидация, безопасность, маршрутизация, базы данных, транзакции, секреты и чек-лист проверки backend.
* [Рельсовый конвейер и система качества (Pipeline & Cookie System)](./references/pipeline-and-reward-engine.md) — 6-фазный State Machine, гейт одобрения пользователя, аудит качества (Cookie System) и защита контекста.


---

## 5. Инструмент механического контроля
 
Исполняемый линтер расположен по пути:
`/Users/tmpr/.agents/skills/go-front/scripts/scan_slop.py`

Использование:
- Внутренняя проверка текста из stdin: `echo "текст" | python3 /Users/tmpr/.agents/skills/go-front/scripts/scan_slop.py -`
- Внутренняя проверка файла: `python3 /Users/tmpr/.agents/skills/go-front/scripts/scan_slop.py путь/к/файлу.md`
- Автозамена тривиальных ошибок: `python3 /Users/tmpr/.agents/skills/go-front/scripts/scan_slop.py путь/к/файлу.md --fix`

**Важно**: Линтер используется агентом автономно для внутренней верификации. Запрещено сбрасывать в чат пользователю технические плашки вида `Чистота текста: 100/100`, если пользователь прямо не запросил аудит.

---

## 6. Память проекта и сессий

### Цель
Агент не должен повторять уже исправленные ошибки и терять контекст при продолжении работы. Все важные решения, ошибки, замечания пользователя и состояние работы сохраняются в памяти проекта.

Память должна позволять продолжить разработку так, чтобы новый агент или новая сессия могли понять историю работы без необходимости восстанавливать её по всему чату.

### Структура памяти
В проекте используется директория:
```text
memory_front/
├── project.md
└── sessions/
    ├── session-001.md
    ├── session-002.md
    └── ...
```

#### `memory_front/project.md`
Содержит постоянную информацию о проекте:
* назначение проекта;
* архитектурные решения и стек;
* структуру проекта и важные ограничения;
* правила реализации и предпочтения пользователя;
* известные проблемы и решения, которые нельзя повторять;
* важные интеграции и состояние основных частей.

Не записывать туда весь диалог — только информацию, имеющую значение для дальнейшей работы.

#### Память отдельной сессии (`memory_front/sessions/session-XXX.md`)
Содержит сжатую историю рабочей сессии:
```markdown
# Session XXX

## Цель
## Контекст
## Что сделали
## Решения
## Ошибки
## Исправления
## Замечания пользователя
## Не повторять
## Незавершённое
## Следующий шаг
## Изменённые файлы
## Важный контекст
```

### Фиксация ошибок и замечаний
Если агент ошибся или пользователь сделал замечание («так больше не делай», «делай проще», «не добавляй это»):
* Записывать **конкретное правило**, а не эмоциональную реакцию.
* Заполнять раздел `Не повторять` конкретными решениями и подходами, которые уже были отклонены.

### Контроль переполнения контекста и переход (`/new`)
Если контекст приближается к лимиту или начинает сжиматься:
1. Обновить файл текущей сессии (`session-XXX.md`).
2. Зафиксировать все важные решения, выполненную работу, ошибки, незавершённые задачи и следующий шаг.
3. Актуализировать долгосрочные правила в `memory_front/project.md`.
4. Использовать команду `/new [краткое описание следующей задачи]`.
5. Новая сессия начинает работу с чтения `memory_front/project.md` и последней сессии.

### Главные 4 вопроса памяти:
1. **Что мы строим?**
2. **Что уже сделали?**
3. **Что уже пробовали и почему это не подходит?**
4. **С чего продолжать работу?**

